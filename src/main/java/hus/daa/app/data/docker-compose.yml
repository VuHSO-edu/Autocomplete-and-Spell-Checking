services:
  vn_words:
    image: postgres:14
    restart: always
    environment:
      POSTGRES_USER: daa
      POSTGRES_PASSWORD: daa123
      POSTGRES_DB: vn_words
    volumes:
      - pgdata:/var/lib/postgresql/data
    ports:
      - "5432:5432"

volumes:
  pgdata:
    driver: local
